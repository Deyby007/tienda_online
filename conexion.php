
    <?php
    //crea una conexion a mi vase de datos con bariables
    $localhost = "127.0.0.1";
    $user = "root";
    $password = "";
    $database = "perfil";
    $conex = mysqli_connect($localhost, $user, $password, $database);
    //si hay algun error en la conexion devuelve un mensaje y termina la ejecucion del script
    /*if (mysqli_connect_error()) {
      echo 'no sirve';
      

      } else {
        echo 'conexion establecida';
      }
      
      */
    
    
    ?>
